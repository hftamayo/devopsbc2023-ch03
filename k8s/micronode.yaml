apiVersion: apps/v1
kind: Deployment
metadata:
  name: ch03micronodejs
spec:
  replicas: 3
  selector:
    matchLabels:
      app: ch03micronodejs
  template:
    metadata:
      labels:
        app: ch03micronodejs
    spec:
      containers:
      - name: ch03micronodejs
        image: ${DOCKER_USERNAME}/ch03micronodejs_result:stable
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: ch03micronodejs-service
spec:
  selector:
    app: ch03micronodejs
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: CluserIP #if I won't use Ingress API: LoadBalancer
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ch03micronodejs-ingress
spec:
  rules:
  - host: my-app.example.com #please update this
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: ch03micronodejs-service
            port:
              number: 80  